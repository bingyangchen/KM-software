{
	"nodes":[
		{"id":"feeb448797aeec75","type":"text","text":"```Python\nimport logging\n\n# root logger configurations\nlogging.basicConfig(\n    level=logging.DEBUG,\n    format=\"[%(asctime)s] %(message)s\",\n    datefmt=\"%Y-%m-%d %H:%M:%S\",\n)\n\nlogging.info(\"How are you?\")  # we can use the root logger to log\n\nlogger = logging.getLogger(\"helloworld\")  # we can also create a logger instance\nlogger.setLevel(logging.INFO)  # and override the root config for each logger\n\nlogger.warning(\"Watch out!\")\nlogger.info(\"I told you so.\")\n```","x":-266,"y":-263,"width":852,"height":476}
	],
	"edges":[]
}